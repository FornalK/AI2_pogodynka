{% extends 'base.html.twig' %}

{% block title %}Measurment{% endblock %}

{% block body %}
    <h1>Measurment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ measurment.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ measurment.date ? measurment.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Temperature</th>
                <td>{{ measurment.temperature }}</td>
            </tr>
            <tr>
                <th>Wind</th>
                <td>{{ measurment.wind }}</td>
            </tr>
            <tr>
                <th>Cloudiness_level</th>
                <td>{{ measurment.cloudinessLevel }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_measurment_index') }}">back to list</a>

    <a href="{{ path('app_measurment_edit', {'id': measurment.id}) }}">edit</a>

    {{ include('measurment/_delete_form.html.twig') }}
{% endblock %}
