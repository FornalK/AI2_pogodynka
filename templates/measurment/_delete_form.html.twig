<form method="post" action="{{ path('app_measurment_delete', {'id': measurment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ measurment.id) }}">
    <button class="btn">Delete</button>
</form>
